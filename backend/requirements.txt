# Oilora Blue AI — Core backend runtime
# Pinned to the exact versions installed and verified on the development
# machine (all 56 backend tests pass against these versions).
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.5
pydantic-settings==2.15.0
python-multipart==0.0.32
python-dotenv==1.2.3